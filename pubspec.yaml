name: chatbot_v1
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.2.0-140.0.dev <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  google_generative_ai: ^0.4.1
  flutter_markdown: ^0.6.19
  flutter_dotenv: ^5.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
